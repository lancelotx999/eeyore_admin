export interface Merchant {
		 	  id: number;
	   		name: string;
	 description: string;
	  	   class: string;
		 parent?: string;
		 address: string;
	geolocation?: string;
	   presence?: string;
		 service: string;
}
